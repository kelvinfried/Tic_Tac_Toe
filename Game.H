#ifndef GAME_H
#define GAME_H

#include <stdlib.h>
#include <time.h>

class Game
{
	private:
	//Attrubutes
		
	public:
	//-----------------------------------------Constructor:
	Game(void );

	//Game(int param );
	//-----------------------------------------Destructor: 
	//Will be used to automatically clean up the dynamic data types and null their points.
	~Game(void );
};

#endif